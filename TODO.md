# 다음에 할 일 (2026-08-24 UX/제품 검토 기준)

> 오늘 UX 검토 + 제품 관점 검토에서 나온 항목 중 **아직 처리 안 한 것만** 정리한 문서입니다.
> 이미 처리된 항목(가짜 AI 마법사 제거, 업무분배 projectId 누락, 모바일 메뉴 버튼 등)과
> 전반적인 아키텍처/알려진 제약사항은 `PROJECT_STATUS.md`를 참고하세요.

## 우선순위 높음 — 실사용에 바로 영향

- [ ] **`/tasks` 페이지의 칸반이 배분승인을 못 함** — `PROJECT_STATUS.md`엔 "칸반 3개를 하나로
  통합했다"고 되어 있지만 실제로는 `tasks/page.tsx`가 자체 칸반을 따로 갖고 있고
  (`src/components/layout/KanbanBoard.tsx`와는 별개), `PENDING_APPROVAL` 카드에 승인/반려
  버튼이 없이 배지만 뜸. PM이 팀원이 매일 쓰는 이 화면에서는 배분 승인을 할 수 없고, 승인은
  `/projects/[id]` 상세 페이지의 칸반이나 `/approvals` 페이지에서만 가능함.
  → 제안: `tasks/page.tsx`의 칸반 뷰를 `KanbanBoard.tsx` 컴포넌트로 교체해서 진짜로 하나로
  통합(지금 "통합했다"고 기록된 상태를 실제로 맞추는 것).

- [ ] **요구사항정의서 재승인 후 업무를 다시 추출하면 중복 생성됨** — `extract-tasks` 라우트가
  같은 `sourceDocumentId`에서 이미 업무를 뽑은 적 있는지 체크하지 않음. 반려→직접수정→재승인
  흐름 후 "업무분배 실행"을 다시 누르면 이전 업무 세트 위에 새 세트가 그대로 추가됨(이미
  진행중/완료된 업무는 낡은 요구사항 기준인 채로 남음).
  → 제안: 재추출 시 기존 미착수(BACKLOG) 업무는 교체, 이미 진행중인 업무는 경고만 띄우고
  건드리지 않는 정책 결정 필요.

## 우선순위 중간 — 눈에 띄는 불편함

- [ ] **인앱 알림이 전혀 없음** — Slack 연동 얘기가 아니라, 반려/배정/승인 같은 일이 생겨도
  당사자가 화면을 직접 열어보기 전엔 알 방법이 없음. `prisma/schema.prisma`에 Notification류
  모델 자체가 없음. 최소한 헤더에 뱃지 카운트라도 필요할 수 있음.
- [ ] **프로젝트를 2개째 만들면 첫 번째가 화면에서 통째로 사라짐** — 프로젝트 전환 UI가 없고
  거의 모든 화면이 `projects[0]`(최신순 첫 번째)만 씀. 단일 프로젝트 전제 자체는 이미 알려진
  제약이지만, "두 번째 프로젝트를 만드는 순간 첫 번째 데이터가 안 보이게 된다"는 구체적 결과는
  새로 발견한 부분.
- [ ] **예상시간이 틀렸을 때 재계획할 UI가 없음** — `TaskDetailModal.tsx`에 진행률 슬라이더만
  있고 일정/시간 재조정은 없음 — 실제 소요시간이 AI 추정을 넘어가도 알 방법이 없음.
- [ ] **`/settings`, `/roles`가 빈 스텁** — `/settings`는 사이드바에 항상 노출되지만 "준비
  중"만 뜸. `/roles`는 어디에도 안 걸려있는 고아 라우트. 실제 화면을 만들지, 링크를 없앨지
  결정 필요(참고: `/api/projects/[id]/settings`엔 Slack/GitHub 연동 저장 API가 이미 있는데
  쓰는 화면이 없음).
- [ ] **히스토리 페이지 빈 상태에 다음 행동 버튼이 없음** — 문서생성 페이지 빈 상태(오늘 추가한
  "첫 프로젝트 만들기" 버튼)와 달리, 히스토리는 프로젝트가 없을 때 그냥 막다른 길.

## 우선순위 낮음 — 다듬으면 좋은 것들

- [ ] **같은 화면을 부르는 이름이 화면마다 다름(부분 해결)** — 모바일 하단 메뉴는 오늘
  "결재"→"승인"으로 고쳤지만, 대시보드 카드("승인 대기"/"검토 요청중")와 승인함 페이지 제목
  ("배분 승인 대기함")은 아직 서로 다른 표현임 — 완전히 하나로 통일할지는 검토 필요.
- [ ] **역할 때문에 수정 불가일 때 화면마다 표시 방식이 다름** — 문서생성 페이지는 자물쇠
  아이콘+설명 문구(예: "검토 중에는 수정할 수 없습니다")를 쓰는데, 직원관리/프로젝트 WBS 뷰는
  그냥 읽기전용 텍스트만 있고 이유 설명이 없음. 문서생성 페이지 패턴으로 통일 제안.
- [ ] **배분승인대기 카드에 승인함으로 가는 링크가 없음** — 칸반에서 멈춰있는 걸 보고도 사이드바
  에서 승인함을 따로 찾아가야 함. 카드에 "승인함에서 보기" 링크 추가하면 좋음.
- [ ] **문서 삭제 불가 아이콘의 상태 표시가 약함** — hover 시 클릭 가능해 보이는데 실제로는
  비활성. 이유는 툴팁(`title=`)뿐이라 터치기기에선 안 보임.

## 참고 (이미 PROJECT_STATUS.md에 기록된 것들 — 여기서 중복 안 함)

서버 사이드 RBAC 없음 · 비밀번호 평문 저장 · DEV 롤 토글 배포 전 제거 필요 · Slack/Git 실제
연동 없음 · 지연 업무 자동 감지 없음 · AI 추천 이력(확정 안 된 후보 포함 전체) 로그 없음 ·
문서 삭제 시 업무 고아화 · `parseProposalDoc(content)!` non-null assertion 패턴
