# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# local sqlite db — contains real user data (plaintext passwords, see PROJECT_STATUS.md)
*.db
*.db-journal

# vendored skill docs (regeneratable, duplicated across .claude/.agents/.windsurf)
/.agents/
/.windsurf/
/.claude/skills/

# one-off dev/debug scripts, not part of the app
/members_page_dump.txt

# 기획/참고용 원본 문서 (WBS 엑셀, 통합기획안) — 코드가 아니라 추적 대상 아님
/Reference/
